import styled from 'styled-components'

import { spaces } from '../../styles/styles'

const StyledButtonRight = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: flex-end;
  align-items: center;
  padding-right: ${spaces.narrow}px;
`

export default StyledButtonRight
