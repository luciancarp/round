import styled from 'styled-components'
import { spaces } from '../styles/styles'

const StyledTitleActions = styled.span`
  display: flex;
  flex-direction: row;
  align-items: center;
  margin-top: ${spaces.wide} px;
`

export default StyledTitleActions
